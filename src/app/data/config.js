// config

'use strict';

module.exports = {

    //----------
    // APP
    //----------

    app: {
    	version: window.global_vars.version,
    	// mode: 'development', // 'development' or 'production'
    	// base_path: 'https://' + window.location.hostname
    }
    
};
